import { inputDataToNumber } from "./util";

export function solution(input: Array<string | number>): number {
  const inputData = inputDataToNumber(input);
  return 1;
}

export function challenge(input: Array<string | number>): number {
  const inputData = inputDataToNumber(input);
  return 0;
}
